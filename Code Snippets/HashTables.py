def hashFunction(x):
    """Example Hash function that returns the remainder of x divided by 2 as the index"""
    return x % 2

#note the above wouldnt be very good because it only has 2 buckets, so it would be very easy to have a collision

class HashTable(object):
    def put(self, key, value):
        pass
    
    def get(key):
        pass
        